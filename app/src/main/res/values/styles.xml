<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dialogplus_card_shadow</item>
        <item name="colorPrimaryDark">@color/dialogplus_card_shadow</item>
        <item name="colorAccent">@color/dialogplus_card_shadow</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <declare-styleable name="RectImageView">
        <attr name="iv_ratio" format="float"/>
    </declare-styleable>
    <!--权限界面是透明主题-->
    <style name="Theme.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
